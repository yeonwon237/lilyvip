# Default (unset, or any value other than "true") is closed: real tier-based
# entitlements apply, so VIP plans actually gate Reader Pro/premium themes/audio.
# Set to true only to temporarily open all implemented Reader and Audio
# features for free during product testing.
VITE_OPEN_BETA=false
